# This is an example file. Copy it to .env and fill in the values.

# PostgreSQL Example: DATABASE_URL=postgresql+psycopg2://user:password@host:port/dbname
# SQLite (default): 
DATABASE_URL=sqlite+pysqlite:///./webhook_events.db

# Celery configuration
CELERY_BROKER_URL=redis://redis:6379/0
CELERY_RESULT_BACKEND=redis://redis:6379/0

# Webhook Secrets
# Generate strong secrets for each provider
# e.g., using `openssl rand -hex 32`
GITHUB_WEBHOOK_SECRET=your_strong_secret_here
STRIPE_WEBHOOK_SECRET=whsec_your_stripe_secret_here
